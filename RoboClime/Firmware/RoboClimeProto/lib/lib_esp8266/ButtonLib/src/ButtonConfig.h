#pragma once

// Конфигурация кнопки по умолчанию
namespace ButtonConfig {
    const uint32_t DEBOUNCE_DELAY_MS = 50;
    const uint32_t LONG_PRESS_DELAY_MS = 1000;
    const bool DEFAULT_PULLUP = true;
}